Context:
- I have an existing calorie tracker application that currently uses hardcoded food data and a local database
- I want to integrate the Google Gemini API (model: gemini-2.5-flash-lite). The API key is already in my .env file as GEMINI\\\_API\\\_KEY.

Instruction:
- Create backend API routes to support two methods using AI for retrieving nutrition data
- Do not modify the frontend at this stage, focus only on getting the API fully functional so it can be integrated later


Input:
- Text-based lookup: A user enters a description of their meal (e.g., "smoked beef with salad"), and the system returns estimated calories and macronutrients
- Image-based lookup: A user uploads an image of their meal, and Gemini analyzes it to identify food items and estimate calories


Output:
- Two fully working API routes: One for text-based queries, One for image uploads
- Both routes should return structured nutrition data generated by Gemini
- No frontend changes
- Updated CLAUDE.md and a new dated entry in docs/ documenting what was implemented, key decisions, and the reasoning behind them